{
  "name": "portfolio-website",
  "version": "0.1.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "postinstall": "cd backend/haiku-backend && npm install && cd ../.. && cd frontend && npm install && npm run build",
    "start": "node backend/haiku-backend/index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "path": "^0.12.7"
  }
}
